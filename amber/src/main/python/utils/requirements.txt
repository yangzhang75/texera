# Python dependencies for the scGPT demo workflow's UDFs (they do `from utils import *`).
# These are the scGPT workflow's runtime deps, NOT Texera core deps. Install them into the
# worker / computing-unit Python env to run the scGPT workflow. Versions verified against the
# venv312 (Python 3.12) where the workflow runs locally.
#
#   <python> -m pip install -r amber/src/main/python/utils/requirements.txt

scgpt==0.2.4
scanpy==1.12.1
anndata==0.12.18
torch==2.3.0
torchtext==0.18.0        # must pair with torch 2.3.0 (scgpt needs torchtext; newer torch has no matching torchtext)
scikit-learn==1.9.0
scipy==1.18.0
numpy==2.1.0
pandas==2.3.3
h5py==3.16.0
matplotlib==3.11.0
seaborn==0.13.2
numba==0.65.1
umap-learn==0.5.12
leidenalg==0.12.0
igraph==1.0.0
scib==1.1.7
tqdm==4.68.3
click==8.4.2
wandb==0.28.0
PyYAML==6.0.3
ipython<9

# Keep Texera's own pins — don't let the packages above silently upgrade them:
protobuf==7.34.1
psutil==5.9.0

# faiss is only used by build_atlas_index_faiss.py (not hit by the current inference chain).
# If that path is exercised, also install:  faiss-cpu   (or faiss-gpu on a GPU node)
